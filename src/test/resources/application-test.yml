spring:
  datasource:
    url: ${TEST_DB_URL:jdbc:postgresql://localhost:55432/shiftcounter_test}
    username: ${TEST_DB_USERNAME:shift_test}
    password: ${TEST_DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

server:
  error:
    include-stacktrace: never
